name: Library Occupancy Scraper

on:
  schedule:
    - cron: '0 13-23 * * *'  # Run every hour from 8 AM to 11 PM
    - cron: '0 0-6 * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        pip install selenium pytz
        sudo apt-get update
        sudo apt-get install -y chromium-browser
    
    - name: Run scraper
      run: python lib-scrapper.py
    
    - name: Commit and push CSV
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add library_occupancy.csv
        git commit -m "Update occupancy data $(date)" || exit 0
        git push
